.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
.env
.DS_Store
.transcript*
backups/
# A CSV of remembered sessions is location history: where you were, when, how often.
# This repo is public. Keep them out of it.
sessions.csv
sessions*.csv
*-sessions.csv
web/node_modules/
web/dist/
# Vite output and the schema it is generated from: both derived, both rebuilt by
# `make ui`. The committed artefact is web/src/api/schema.d.ts, so the frontend can be
# typechecked without a Python environment.
src/gogo/static/app/
openapi.json
